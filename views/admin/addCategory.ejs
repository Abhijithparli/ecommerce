<h1 class="text-3xl font-bold mb-8 text-white">

  Add Category

</h1>

  <!-- ADD CATEGORY FORM -->
  <div class="bg-gray-800 rounded-xl p-6 mb-8 shadow-lg">

    <h2 class="text-xl font-semibold mb-4 text-white">
      Add New Category
    </h2>

    <form action="/admin/categories/add" method="POST">

      <!-- CATEGORY NAME -->
      <div class="mb-4">
        <label class="block mb-2 text-sm text-white">Category Name</label>

        <input
          type="text"
          name="name"
          placeholder="Enter category name"
         class="w-full px-4 py-3 rounded-lg bg-gray-200 border border-gray-600 text-white placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-orange-500"
          required
        >
      </div>

      <!-- DESCRIPTION -->
      <div class="mb-4">
        <label class="block mb-2 text-sm text-white">Description</label>

        <textarea
          name="description"
          rows="3"
          placeholder="Enter category description"
          class="w-full px-4 py-3 rounded-lg bg-gray-700 border border-gray-600 text-white placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-orange-500"
        ></textarea>
      </div>

      <!-- BUTTON -->
      <button
        class="bg-orange-500 hover:bg-orange-600 transition px-6 py-3 rounded-lg font-semibold"
      >
        Add Category
      </button>

    </form>